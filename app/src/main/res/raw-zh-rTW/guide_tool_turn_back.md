「折返」工具可以用於提醒你何時在遠足期間折返以在特定時間返回。

## 返回時間
你可以透過點擊輸入欄並從時間選擇器中選擇時間來設定返回時間。返回時間是指如果你現在開始遠足，你想要返回目前位置的時間。

設定好返回時間後，遠足中途之前會顯示警報以提醒你準時返回。螢幕頂部會顯示距離返回時間的時間。

### 天黑前返回
如果你點擊「天黑前返回」按鈕，返回時間將會設定為日落。如果你想確保在天黑之前返回，這非常有用。

## 取消警報
你可以透過點擊螢幕底部的「取消」按鈕來取消警報。這將清除返回時間和警報。
